# --- Dependencies / build artifacts ---
node_modules/
dist/
.wrangler/

# --- Env / secrets ---
.env
.env.*
!.env.example

# --- brochure-site-transform working artifact (do NOT commit) ---
.transform-state.json

# --- designer-src is TRACKED (skill requirement) EXCEPT regenerable caches ---
# .local/ is a vendored pnpm content-addressable store (~428M) — install cache,
# not designer source. Everything else under designer-src/ stays committed.
designer-src/.local/
designer-src/**/node_modules/
designer-src/**/dist/

# --- OS cruft ---
.DS_Store
